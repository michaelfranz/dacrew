# Python virtual environments
            venv/
            env/
            .venv/
            .env/
            __pycache__/
            *.pyc
            *.pyo
            *.pyd
            .Python

            # IDE and editor files
            .vscode/
            .idea/
            *.swp
            *.swo
            *~

            # Version control
            .git/
            .gitignore

            # OS generated files
            .DS_Store
            .DS_Store?
            ._*
            .Spotlight-V100
            .Trashes
            ehthumbs.db
            Thumbs.db

            # Build artifacts
            build/
            dist/
            *.egg-info/
            .eggs/

            # Test artifacts
            .pytest_cache/
            .coverage
            htmlcov/
            .tox/
            .nox/
            test-results.xml
            coverage.xml

            # Documentation
            docs/_build/

            # Logs
            *.log
            logs/

            # Data directories (if you have large data files)
            data/
            *.db
            *.sqlite3

            # Temporary files
            tmp/
            temp/
            *.tmp

            # Large ML models or datasets
            *.model
            *.bin
            models/
            datasets/

            # Docker files (avoid recursive copying)
            Dockerfile*
            docker-compose*.yml

            # GitHub workflows (not needed in container)
            .github/

            # Package manager files that might be large
            node_modules/
            .npm/
# Core testing dependencies
pytest==8.1.1
pytest-cov
pytest-timeout
pytest-xdist
pytest-html

# Code quality
black==24.4.0
isort==5.13.2
mypy==1.10.0

# Essential dependencies that your tests actually need
jira==3.5.2
python-dotenv==1.0.0
click==8.1.8
rich==13.7.1
typer==0.12.3
pydantic==2.11.7

# Data processing (lighter versions)
pandas==2.2.2
numpy==1.26.4

# Only include vector DB if your tests need it
chromadb==0.4.15
sentence-transformers==2.7.0

# Skip the heavy conflicting packages for CI:
# crewai==0.140.0           # Causes dependency conflicts
# langchain==0.3.26         # Heavy and conflicts
# transformers==4.40.1      # 2GB+ download
# torch==2.3.0              # 3GB+ download
# openai==1.93.0            # Not needed for unit tests